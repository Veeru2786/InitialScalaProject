package Demo

/*
Higher order functions are those whose take function as argument and return as well. this is possible because in scala function are first class values
These functions allow to create lambda function or Anonymous function.
 */
object HigherOrderFunction {
  def main(args: Array[String]): Unit ={


  }

}
